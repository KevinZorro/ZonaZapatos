[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npx serve dist --listen tcp://0.0.0.0:$PORT --single"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
